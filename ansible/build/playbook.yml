- name: Build and push
  hosts: Localhost
  connection: local
  tasks:
      - name: Build frontend image
        command: >
            docker build -t divancode74/coduels-frontend:{{ version }} ../../
      - name: Push frontend image
        command: >
            docker push divancode74/coduels-frontend:{{ version }}
